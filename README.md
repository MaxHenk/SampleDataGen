# SampleDataGen
A simple tool that takes into input a JSON containing some basic structure and outputting a JSON containing randomly generated objects furthering the original JSON data to make wider sample datasets.
